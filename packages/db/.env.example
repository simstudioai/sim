# Database URL (Required for migrations and database operations)
DATABASE_URL="postgresql://postgres:password@localhost:5432/simstudio"

# Database Schema (Optional) - defaults to SQLUser for primary backend (IRIS) if not specified
# Reference backend (PostgreSQL) ignores DB_DEFAULT_SCHEMA
# DB_DEFAULT_SCHEMA=SQLUser
# DB_METADATA_SCHEMA=drizzle
