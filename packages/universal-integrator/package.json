{
  "name": "@sim/universal-integrator",
  "version": "8.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Deterministic integration generator (local LLM) — chain of algorithms that GUARANTEES complete API coverage",
  "bin": {
    "sim-integrate": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "integrate": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@sim/tsconfig": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
