{
  "name": "@sim/universal-integrator",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Universal sim.ai integration agent (LangChain + Claude). Takes ANY service URL / name / SDK and generates a COMPLETE Block + Tools integration for apps/sim, covering every API method.",
  "bin": {
    "sim-integrate": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "integrate": "bun run src/index.ts"
  },
  "dependencies": {
    "@langchain/anthropic": "^0.3.0",
    "@langchain/core": "^0.3.0",
    "langchain": "^0.3.0"
  },
  "devDependencies": {
    "@sim/tsconfig": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
