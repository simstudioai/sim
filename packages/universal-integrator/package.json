{
  "name": "@sim/universal-integrator",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Universal sim.ai integration agent (Claude Agent SDK). Takes ANY service URL / name / SDK and generates a COMPLETE Block + Tools integration for apps/sim, covering every API method.",
  "bin": {
    "sim-integrate": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "integrate": "bun run src/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.183"
  },
  "devDependencies": {
    "@sim/tsconfig": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
