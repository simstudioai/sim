{
  "name": "apps-host",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../sim/.env run src/server.ts",
    "start": "bun run src/server.ts",
    "type-check": "tsc --noEmit",
    "test": "bun test src"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  }
}
