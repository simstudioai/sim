---
title: Copilot
---

import { Callout } from 'fumadocs-ui/components/callout'
import { Card, Cards } from 'fumadocs-ui/components/card'
import { Image } from '@/components/ui/image'
import { MessageCircle, Hammer, ListChecks, Zap, Globe, Paperclip, History, RotateCcw, Brain } from 'lucide-react'

Copilot es tu asistente integrado en el editor que te ayuda a crear y editar flujos de trabajo. Puede:

- **Explicar**: Responder preguntas sobre Sim y tu flujo de trabajo actual
- **Guiar**: Sugerir ediciones y mejores prácticas
- **Construir**: Añadir bloques, conectar variables y configurar ajustes
- **Depurar**: Analizar problemas de ejecución y optimizar rendimiento

<Callout type="info">
  Copilot es un servicio gestionado por Sim. Para implementaciones autoalojadas:
  1. Ve a [sim.ai](https://sim.ai) → Configuración → Copilot y genera una clave API de Copilot
  2. Establece `COPILOT_API_KEY` en tu entorno autoalojado
</Callout>

## Modos

Cambia entre modos usando el selector de modo en la parte inferior del área de entrada.

<Cards>
  <Card
    title={
      <span className="inline-flex items-center gap-2">
        <MessageCircle className="h-4 w-4 text-muted-foreground" />
        Preguntar
      </span>
    }
  >
    <div className="m-0 text-sm">
      Modo Q&A para explicaciones, orientación y sugerencias sin realizar cambios en tu flujo de trabajo.
    </div>
  </Card>
  <Card
    title={
      <span className="inline-flex items-center gap-2">
        <Hammer className="h-4 w-4 text-muted-foreground" />
        Construir
      </span>
    }
  >
    <div className="m-0 text-sm">
      Modo de construcción de flujo de trabajo. Copilot puede añadir bloques, conectar variables, editar configuraciones y depurar problemas.
    </div>
  </Card>
  <Card
    title={
      <span className="inline-flex items-center gap-2">
        <ListChecks className="h-4 w-4 text-muted-foreground" />
        Planificar
      </span>
    }
  >
    <div className="m-0 text-sm">
      Crea un plan de implementación paso a paso para tu flujo de trabajo sin realizar cambios. Te ayuda a pensar en el enfoque antes de construir.
    </div>
  </Card>
</Cards>

## Modelos

Selecciona tu modelo de IA preferido usando el selector de modelos en la parte inferior derecha del área de entrada.

**Modelos disponibles:**
- **Anthropic**: Claude Opus 4.6 (predeterminado), Claude Opus 4.5, Claude Sonnet 4.5, Claude Haiku 4.5
- **OpenAI**: GPT-5.4, GPT-5.2, GPT-5.1, GPT-5, GPT-4o, GPT-4.1, o3, o4-mini
- **Google**: Gemini 2.5 Pro, Gemini 2.5 Flash, Gemini 2.0 Flash
- **Otros proveedores**: Groq, Cerebras, DeepSeek, Mistral, xAI, Azure OpenAI, OpenRouter
- **Modelos locales**: modelos compatibles con Ollama o VLLM

Elige según tus necesidades: modelos más rápidos para tareas simples, modelos más capaces para flujos de trabajo complejos.

## Menú contextual (@)

Usa el símbolo `@` para hacer referencia a recursos y proporcionar a Copilot más contexto:

| Referencia | Descripción |
|-----------|-------------|
| **Chats** | Conversaciones previas con Copilot |
| **Flujos de trabajo** | Cualquier flujo de trabajo en tu espacio de trabajo |
| **Bloques de flujo de trabajo** | Bloques en el flujo de trabajo actual |
| **Bloques** | Tipos de bloques y plantillas |
| **Conocimiento** | Documentos subidos y bases de conocimientos |
| **Documentación** | Documentación de Sim |
| **Plantillas** | Plantillas de flujo de trabajo |
| **Registros** | Registros de ejecución y resultados |

Escribe `@` en el campo de entrada para abrir el menú contextual, luego busca o explora para encontrar lo que necesitas.

## Comandos de barra (/)

Usa comandos de barra para acciones rápidas:

| Comando | Descripción |
|---------|-------------|
| `/fast` | Ejecución en modo rápido |
| `/research` | Modo de investigación y exploración |
| `/actions` | Ejecutar acciones de agente |

**Comandos web:**

| Comando | Descripción |
|---------|-------------|
| `/search` | Buscar en la web |
| `/read` | Leer una URL específica |
| `/scrape` | Raspar contenido de página web |
| `/crawl` | Rastrear múltiples páginas |

Escribe `/` en el campo de entrada para ver los comandos disponibles.

## Gestión de chats

### Comenzar una nueva conversación

Haz clic en el botón **+** en el encabezado de Copilot para iniciar una nueva conversación.

### Historial de chat

Haz clic en **Historial** para ver conversaciones anteriores agrupadas por fecha. Puedes:
- Haz clic en un chat para reanudarlo
- Eliminar chats que ya no necesites

### Editar mensajes

Pasa el cursor sobre cualquiera de tus mensajes y haz clic en **Editar** para modificar y reenviar. Esto es útil para refinar tus prompts.

### Cola de mensajes

Si envías un mensaje mientras Copilot sigue respondiendo, se pone en cola. Puedes:
- Ver mensajes en cola en el panel expandible de cola
- Enviar un mensaje en cola inmediatamente (aborta la respuesta actual)
- Eliminar mensajes de la cola

## Archivos adjuntos

Haz clic en el icono de adjunto para subir archivos con tu mensaje. Los tipos de archivo compatibles incluyen:
- Imágenes (se muestran miniaturas de vista previa)
- PDFs
- Archivos de texto, JSON, XML
- Otros formatos de documentos

Los archivos se muestran como miniaturas en las que se puede hacer clic y que se abren en una nueva pestaña.

## Puntos de control y cambios

Cuando Copilot realiza cambios en tu flujo de trabajo, guarda puntos de control para que puedas revertirlos si es necesario.

### Ver puntos de control

Pasa el cursor sobre un mensaje de Copilot y haz clic en el icono de puntos de control para ver los estados guardados del flujo de trabajo para ese mensaje.

### Revertir cambios

Haz clic en **Revertir** en cualquier punto de control para restaurar tu flujo de trabajo a ese estado. Un diálogo de confirmación te advertirá que esta acción no se puede deshacer.

### Aceptar cambios

Cuando Copilot propone cambios, puedes:
- **Aceptar**: aplicar los cambios propuestos (`Mod+Shift+Enter`)
- **Rechazar**: descartar los cambios y mantener tu flujo de trabajo actual

## Bloques de pensamiento

Para solicitudes complejas, Copilot puede mostrar su proceso de razonamiento en bloques de pensamiento expandibles:

- Los bloques se expanden automáticamente mientras Copilot está pensando
- Haz clic para expandir/contraer manualmente
- Muestra la duración del proceso de pensamiento
- Te ayuda a entender cómo Copilot llegó a su solución

## Selección de opciones

Cuando Copilot presenta múltiples opciones, puedes seleccionar usando:

| Control | Acción |
|---------|--------|
| **1-9** | Seleccionar opción por número |
| **Flecha arriba/abajo** | Navegar entre opciones |
| **Enter** | Seleccionar opción resaltada |

Las opciones seleccionadas están resaltadas; las opciones no seleccionadas aparecen tachadas.

## Atajos de teclado

| Atajo | Acción |
|----------|--------|
| `@` | Abrir menú contextual |
| `/` | Abrir comandos de barra |
| `Arrow Up/Down` | Navegar elementos del menú |
| `Enter` | Seleccionar elemento del menú |
| `Esc` | Cerrar menús |
| `Mod+Shift+Enter` | Aceptar cambios de Copilot |

## Límites de uso

El uso de Copilot se factura por token del LLM subyacente y cuenta hacia el uso de créditos de tu plan. Si alcanzas tu límite de uso, habilita facturación bajo demanda desde Configuración → Suscripción para continuar usando Copilot más allá de los créditos incluidos en tu plan.

<Callout type="info">
  Consulta la [página de cálculo de costos](/execution/costs) para detalles de facturación y planes.
</Callout>
## Copilot MCP

Puedes usar Copilot como servidor MCP en tu editor o cliente de IA favorito. Esto te permite construir, probar, desplegar y gestionar flujos de trabajo de Sim directamente desde herramientas como Cursor, Claude Code, Claude Desktop y VS Code.

### Generar una clave API de Copilot

Para conectarte al servidor MCP de Copilot, necesitas una **clave API de Copilot**:

1. Ve a [sim.ai](https://sim.ai) e inicia sesión
2. Navega a **Configuración** → **Copilot**
3. Haz clic en **Generar clave API**
4. Copia la clave — solo se muestra una vez

La clave se verá como `sk-sim-copilot-...`. Usarás esto en la configuración a continuación.

### Cursor

Agrega lo siguiente a tu `.cursor/mcp.json` (nivel de proyecto) o configuración global de MCP de Cursor:

```json
{
  "mcpServers": {
    "sim-copilot": {
      "url": "https://www.sim.ai/api/mcp/copilot",
      "headers": {
        "X-API-Key": "YOUR_COPILOT_API_KEY"
      }
    }
  }
}
```

Reemplaza `YOUR_COPILOT_API_KEY` con la clave que generaste anteriormente.

### Claude Code

Ejecuta el siguiente comando para añadir el servidor MCP de Copilot:

```bash
claude mcp add sim-copilot \
  --transport http \
  https://www.sim.ai/api/mcp/copilot \
  --header "X-API-Key: YOUR_COPILOT_API_KEY"
```

Reemplaza `YOUR_COPILOT_API_KEY` con tu clave.

### Claude Desktop

Claude Desktop requiere [`mcp-remote`](https://www.npmjs.com/package/mcp-remote) para conectarse a servidores MCP basados en HTTP. Añade lo siguiente a tu archivo de configuración de Claude Desktop (`~/Library/Application Support/Claude/claude_desktop_config.json` en macOS):

```json
{
  "mcpServers": {
    "sim-copilot": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://www.sim.ai/api/mcp/copilot",
        "--header",
        "X-API-Key: YOUR_COPILOT_API_KEY"
      ]
    }
  }
}
```

Reemplaza `YOUR_COPILOT_API_KEY` con tu clave.

### VS Code

Añade lo siguiente a tu `settings.json` de VS Code o al `.vscode/settings.json` del espacio de trabajo:

```json
{
  "mcp": {
    "servers": {
      "sim-copilot": {
        "type": "http",
        "url": "https://www.sim.ai/api/mcp/copilot",
        "headers": {
          "X-API-Key": "YOUR_COPILOT_API_KEY"
        }
      }
    }
  }
}
```

Reemplaza `YOUR_COPILOT_API_KEY` con tu clave.

<Callout type="info">
  Para implementaciones auto-alojadas, reemplaza `https://www.sim.ai` con tu URL de Sim auto-alojada.
</Callout>

import { FAQ } from '@/components/ui/faq'

<FAQ items={[
  { question: "¿Cuál es la diferencia entre el modo Preguntar, Construir y Planificar?", answer: "Copilot tiene tres modos. El modo Preguntar es un modo Q&A de solo lectura para explicaciones, orientación y sugerencias sin realizar cambios en tu flujo de trabajo. El modo Construir permite que Copilot modifique activamente tu flujo de trabajo añadiendo bloques, conectando variables, editando configuraciones y depurando problemas. El modo Planificar crea un plan de implementación paso a paso para tu solicitud sin realizar cambios, para que puedas revisar el enfoque antes de comprometerte. Usa Preguntar cuando quieras aprender o explorar ideas, Planificar cuando quieras ver un enfoque propuesto primero, y Construir cuando quieras que Copilot realice cambios directamente." },
  { question: "¿Tiene Copilot acceso a mi flujo de trabajo completo cuando responde preguntas?", answer: "Copilot tiene acceso al flujo de trabajo que estás editando actualmente como contexto. También puedes usar el menú contextual @ para hacer referencia a otros flujos de trabajo, chats anteriores, registros de ejecución, bases de conocimientos, documentación y plantillas para proporcionar a Copilot contexto adicional para tu solicitud." },
  { question: "¿Cómo uso Copilot desde un editor externo como Cursor o VS Code?", answer: "Puedes usar Copilot como servidor MCP desde editores externos. Primero, genera una clave API de Copilot desde Configuración > Copilot en sim.ai. Luego, añade la configuración del servidor MCP a tu editor usando el endpoint https://www.sim.ai/api/mcp/copilot con tu clave API en el encabezado X-API-Key. Los ejemplos de configuración están disponibles para Cursor, Claude Code, Claude Desktop y VS Code." },
  { question: "¿Puedo revertir cambios que Copilot hizo en mi flujo de trabajo?", answer: "Sí. Cuando Copilot realiza cambios en modo Construir, guarda puntos de control del estado de tu flujo de trabajo. Puedes pasar el cursor sobre un mensaje de Copilot y hacer clic en el icono de puntos de control para ver estados guardados, luego hacer clic en Revertir en cualquier punto de control para restaurar tu flujo de trabajo. Ten en cuenta que revertir no se puede deshacer, así que revisa el punto de control antes de confirmar." },
  { question: "¿Cómo funciona la facturación de Copilot?", answer: "El uso de Copilot se factura por token del LLM subyacente y cuenta hacia el uso de créditos de tu plan. Los modelos más capaces como Claude Opus cuestan más por token que los modelos más ligeros como Haiku. Si alcanzas tu límite de uso, puedes habilitar facturación bajo demanda desde Configuración > Suscripción para continuar usando Copilot." },
  { question: "¿Qué hacen los comandos de barra como /research y /search?", answer: "Los comandos de barra activan comportamientos especializados. /fast habilita ejecución en modo rápido, /research activa un modo de investigación y exploración, y /actions ejecuta acciones de agente. Los comandos web como /search, /read, /scrape y /crawl permiten que Copilot interactúe con la web para buscar información, leer URLs, raspar contenido de página o rastrear múltiples páginas para recopilar contexto para tu solicitud." },
  { question: "¿Cómo configuro Copilot para una implementación auto-alojada?", answer: "Para implementaciones auto-alojadas, ve a sim.ai > Configuración > Copilot y genera una clave API de Copilot. Luego, establece la variable de entorno COPILOT_API_KEY en tu entorno auto-alojado. Copilot es un servicio gestionado por Sim, por lo que la instancia auto-alojada se comunica con los servidores de Sim para procesar solicitudes." },
]} />


