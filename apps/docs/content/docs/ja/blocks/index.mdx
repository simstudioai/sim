---
title: 概要
description: AIワークフローの構成要素
---

import { Card, Cards } from 'fumadocs-ui/components/card'
import { Step, Steps } from 'fumadocs-ui/components/steps'
import { Tab, Tabs } from 'fumadocs-ui/components/tabs'
import { Callout } from 'fumadocs-ui/components/callout'
import { Video } from '@/components/ui/video'
import { FAQ } from '@/components/ui/faq'

ブロックはAIワークフローを作成するために接続する構成要素です。AIモデルとのチャット、APIコールの実行、データ処理など、特定のタスクを処理する専門モジュールと考えてください。

<div className="w-full max-w-2xl mx-auto overflow-hidden rounded-lg">
  <Video src="connections.mp4" width={700} height={450} />
</div>

## 主要ブロックタイプ

Simは、AIワークフローの中核機能を処理する必須のブロックタイプを提供します：

### 処理ブロック
- **[エージェント](/blocks/agent)** - AIモデル（OpenAI、Anthropic、Google、ローカルモデル）とチャット
- **[ファンクション](/blocks/function)** - カスタムJavaScript/TypeScriptコードを実行
- **[API](/blocks/api)** - HTTP要求を介して外部サービスに接続
- **[メモリ](/blocks/memory)** - AIエージェント用の会話メモリを保存および取得

### ロジックブロック
- **[条件](/blocks/condition)** - ブール式に基づいてワークフローパスを分岐
- **[ルーター](/blocks/router)** - AIを使用してリクエストを異なるパスにインテリジェントにルーティング
- **[評価者](/blocks/evaluator)** - AIを使用してコンテンツの品質を評価・採点

### コントロールフローブロック
- **[変数](/blocks/variables)** - ワークフロースコープの変数を設定および管理
- **[待機](/blocks/wait)** - 指定した時間だけワークフロー実行を一時停止
- **[ヒューマンインザループ](/blocks/human-in-the-loop)** - 続行する前に人間の承認とフィードバックを待つ

### 出力ブロック
- **[レスポンス](/blocks/response)** - ワークフローからの最終結果をフォーマットして返す
- **[ウェブフック（アウトバウンド）](/blocks/webhook-request)** - 外部ウェブフックにオプションのHMAC署名付きでHTTP POSTリクエストを送信

## ブロックの仕組み

各ブロックには3つの主要コンポーネントがあります：

**入力**：他のブロックやユーザー入力からブロックに入ってくるデータ
**設定**：ブロックの動作を制御する設定
**出力**：ブロックが生成し、他のブロックが使用するデータ

<Steps>
  <Step>
    <strong>入力を受け取る</strong>：ブロックは接続されたブロックまたはユーザー入力からデータを受け取ります
  </Step>
  <Step>
    <strong>処理</strong>：ブロックは設定に従って入力を処理します
  </Step>
  <Step>
    <strong>結果を出力</strong>：ブロックはワークフロー内の次のブロックのための出力データを生成します
  </Step>
</Steps>

## ブロックの接続

ブロックを接続してワークフローを作成します。あるブロックの出力は、別のブロックの入力になります：

- **ドラッグして接続**：出力ポートから入力ポートにドラッグ
- **複数の接続**：1つの出力を複数の入力に接続可能
- **分岐パス**：一部のブロックは条件に基づいて異なるパスにルーティング可能

<div className="w-full max-w-2xl mx-auto overflow-hidden rounded-lg">
  <Video src="connections.mp4" width={700} height={450} />
</div>

## 一般的なパターン

### 順次処理
各ブロックが前のブロックの出力を処理するチェーン状にブロックを接続します：

```
User Input → Agent → Function → Response
```

### 条件分岐
条件またはルーターブロックを使用して異なるパスを作成します：

```
User Input → Router → Agent A (for questions)
                   → Agent B (for commands)
```

### 品質管理
評価ブロックを使用して出力を評価およびフィルタリングします：

```
Agent → Evaluator → Condition → Response (if good)
                              → Agent (retry if bad)
```

## 正しいブロックを選ぶ

| 実行したいこと | 使用するブロック |
|---|---|
| AIモデルとチャットするか、LLM応答を取得したい | [エージェント](/blocks/agent) |
| カスタムJavaScriptまたはPythonコードを実行 | [ファンクション](/blocks/function) |
| 外部REST APIを呼び出す | [API](/blocks/api) |
| 真偽値条件に基づいて分岐 | [条件](/blocks/condition) |
| AIの判断を使用してリクエストをルーティング | [ルーター](/blocks/router) |
| コンテンツの品質をスコア化または評価 | [評価者](/blocks/evaluator) |
| 続行する前に人間の承認が必要 | [ヒューマンインザループ](/blocks/human-in-the-loop) |
| ステップ間の時間遅延を追加 | [待機](/blocks/wait) |
| ワークフロー変数を設定または更新 | [変数](/blocks/variables) |
| ワークフローから最終出力を返す | [レスポンス](/blocks/response) |
| アウトバウンドウェブフックPOSTリクエストを送信 | [ウェブフック](/blocks/webhook) |
| 別のワークフローを子として実行 | [ワークフロー](/blocks/workflow) |
| 会話メモリを保存/取得 | [メモリ](/blocks/memory) |
| キャンバスにノートまたはドキュメントを追加 | [ノート](/blocks/note) |
| 安全またはPIIについてコンテンツを検証 | [ガードレール](/blocks/guardrails) |
| ループでブロックを実行 | [ループ](/blocks/loop) |
| ブロックを並行実行 | [並列](/blocks/parallel) |

<Callout type="info">
  サードパーティサービス統合（Gmail、Slack、GitHubなど）については、[ツール＆統合](/tools)カタログを参照してください。各統合には独自の専用ブロックがあります。

  [思考](/blocks/thinking)ブロックは高度な推論ワークフロー向けに利用可能ですが、標準ブロックツールバーから非表示です。
</Callout>

## ブロック設定

各ブロックタイプには特定の設定オプションがあります：

**すべてのブロック**:
- 入出力接続
- エラー処理の動作
- 実行タイムアウト設定

**AIブロック** (エージェント、ルーター、評価者):
- モデル選択（OpenAI、Anthropic、Google、ローカル）
- APIキーと認証
- 温度やその他のモデルパラメータ
- システムプロンプトと指示

**ロジックブロック** (条件、関数):
- カスタム式またはコード
- 変数参照
- 実行環境設定

**統合ブロック** (API、レスポンス):
- エンドポイント構成
- ヘッダーと認証
- リクエスト/レスポンスのフォーマット

<Cards>
  <Card title="エージェントブロック" href="/blocks/agent">
    AIモデルに接続し、インテリジェントな応答を作成
  </Card>
  <Card title="ファンクションブロック" href="/blocks/function">
    カスタムコードを実行してデータを処理および変換
  </Card>
  <Card title="APIブロック" href="/blocks/api">
    外部サービスやAPIと統合
  </Card>
  <Card title="条件ブロック" href="/blocks/condition">
    データ評価に基づいた分岐ロジックを作成
  </Card>
  <Card title="ヒューマンインザループブロック" href="/blocks/human-in-the-loop">
    続行する前に人間の承認とフィードバックを待つ
  </Card>
  <Card title="変数ブロック" href="/blocks/variables">
    ワークフロースコープの変数を設定および管理
  </Card>
  <Card title="待機ブロック" href="/blocks/wait">
    指定した時間だけワークフロー実行を一時停止
  </Card>
</Cards>

## ブロックのバージョン

Simが進化するにつれて、ブロックは更新と改善を受け取ります。後方互換性を維持するため、ブロックの大幅なアップデートは元のブロックを置き換える代わりに、新しいバージョンのブロック（v2、v3など）を作成します。

### バージョン管理の仕組み

ブロックが主要機能の更新またはAPI変更を受け取ると、バージョンサフィックス付きの新しいバージョンが作成されます（例：`file`は`file_v2`になります）。元のバージョンは既存のワークフロー向けに使用可能なままです。

### どのバージョンを使用するか

- **新規ワークフロー**：コパイロットはワークフローを構築する際に最新バージョンを自動選択します
- **既存ワークフロー**：構築されたバージョンを使用し続けます。明示的にアップグレードする場合を除き、変更は不要です
- **手動アップグレード**：ワークフロー設定を編集してブロックを新しいバージョンに更新できます

### バージョン管理されたブロック

以下のブロックは複数のバージョンが利用可能です：

Confluence (v2) · Cursor (v2) · File (v2, v3) · Fireflies (v2) · GitHub (v2) · Gmail (v2) · Google Calendar (v2) · Google Sheets (v2) · Google Slides (v2) · Intercom (v2) · Kalshi (v2) · Microsoft Excel (v2) · Mistral Parse (v2, v3) · Notion (v2) · Pulse (v2) · Reducto (v2) · Router (v2) · Speech-to-Text (v2) · Textract (v2) · Video Generator (v2) · Vision (v2)

<FAQ items={[
  { question: "Simで利用可能なブロックタイプの数はいくつですか？", answer: "Simは、190以上のブロックがレジストリにあります。コア ワークフロー ブロック（エージェント、ファンクション、条件、ルーターなど）、サードパーティ サービス用の統合ブロック（Gmail、Slack、GitHub、Notion など）、外部イベント（ウェブフック、スケジュールなど）からワークフローを開始するトリガーブロックを含みます。ループと並列実行は、スタンドアロンのレジストリブロックではなく、キャンバス上のコンテナ構築として実行エンジンに組み込まれています。" },
  { question: "1つのブロックの出力を複数のダウンストリームブロックに接続できますか？", answer: "はい。単一の出力ポートを異なるブロック上の複数の入力ポートに接続できます。これにより、ブロックを複製する必要なく、1つの処理ステップから複数の並列パスにデータをフォーアウトできます。" },
  { question: "ワークフロー中央のブロックが失敗したらどうなりますか？", answer: "ブロックがエラーを検出すると、ワークフローはそのパスに沿った実行を停止します。エラー処理をサポートするブロック（ルーターなど）はエラーパスにルーティングでき、ワークフロー全体を停止する代わりに、障害を適切に処理できます。" },
  { question: "処理ブロックとロジックブロックの違いは何ですか？", answer: "処理ブロック（エージェント、ファンクション、API）はデータを変換または生成します。これらが実際の作業を行います。ロジックブロック（条件、ルーター、評価者）は、データを変更することなく、ワークフローがどのパスを取るべきかについての決定を行います。" },
  { question: "異なるカテゴリからのブロックを1つのワークフローで使用できますか？", answer: "絶対に。一般的なワークフローは複数のカテゴリからのブロックを組み合わせています。例えば、ワークフロー を開始するトリガーブロック、入力を処理するエージェント ブロック、ロジックを分岐するための条件ブロック、結果を送信するメールなどの統合ブロックを使用できます。" },
  { question: "他のブロックを内部に保持できるコンテナブロックはありますか？", answer: "はい。ループと並列は実行エンジン構築であり、キャンバス上のコンテナ領域として表示されます。その中に他のブロックをドラッグします。ループコンテナはその内側のブロックを繰り返し実行し、並列コンテナはその内側のブロックを複数のブランチで同時実行します。レジストリブロックとは異なり、これらは実行エンジンによって直接処理されます。" },
]} />
