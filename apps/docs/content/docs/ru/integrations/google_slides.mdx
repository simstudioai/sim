---
title: Google Slides
description: Чтение, запись и создание презентаций
---

import { BlockInfoCard } from "@/components/ui/block-info-card"

<BlockInfoCard 
  type="google_slides_v2"
  color="#FFFFFF"
/>

{/* MANUAL-CONTENT-START:intro */}
[Google Slides](https://slides.google.com) is a dynamic cloud-based presentation application that allows users to create, edit, collaborate on, and present slideshows in real-time. As part of Google's productivity suite, Google Slides offers a flexible platform for designing engaging presentations, collaborating with others, and sharing content seamlessly through the cloud.

Learn how to integrate the Google Slides tools in Sim to effortlessly manage presentations as part of your automated workflows. With Sim, you can read, write, create, and update Google Slides presentations directly through your agents and automated processes, making it easy to deliver up-to-date information, generate custom reports, or produce branded decks programmatically.

With Google Slides, you can:

- **Create and edit presentations**: Design visually appealing slides with themes, layouts, and multimedia content
- **Collaborate in real-time**: Work simultaneously with teammates, comment, assign tasks, and receive live feedback on presentations
- **Present anywhere**: Display presentations online or offline, share links, or publish to the web
- **Add images and rich content**: Insert images, graphics, charts, and videos to make your presentations engaging
- **Integrate with other services**: Connect seamlessly with Google Drive, Docs, Sheets, and other third-party tools
- **Access from any device**: Use Google Slides on desktops, laptops, tablets, and mobile devices for maximum flexibility

In Sim, the Google Slides integration enables your agents to interact directly with presentation files programmatically. Automate tasks like reading slide content, inserting new slides or images, replacing text throughout a deck, generating new presentations, and retrieving slide thumbnails. This empowers you to scale content creation, keep presentations up-to-date, and embed them into automated document workflows. By connecting Sim with Google Slides, you facilitate AI-driven presentation management—making it easy to generate, update, or extract information from presentations without manual effort.
{/* MANUAL-CONTENT-END */}


## Инструкции по использованию

Build, edit, and export branded Google Slides presentations end-to-end. Copy a template, replace text and image tokens, embed Sheets charts, style text and shapes with brand fonts and colors, manage tables and layouts, group elements, run atomic batch updates, and export to PDF or PPTX.



## Действия

### `google_slides_read`

Read content from a Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `slides` | json | Array of slides with their content |
| `metadata` | json | Presentation metadata including ID, title, and URL |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `title` | string | The presentation title |
|   ↳ `pageSize` | object | Presentation page size |
|     ↳ `width` | json | Page width as a Dimension object |
|     ↳ `height` | json | Page height as a Dimension object |
|   ↳ `mimeType` | string | The mime type of the presentation |
|   ↳ `url` | string | URL to open the presentation |

### `google_slides_write`

Write or update content in a Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `content` | string | Да | The content to write to the slide |
| `slideIndex` | number | Нет | The index of the slide to write to \(defaults to first slide\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updatedContent` | boolean | Indicates if presentation content was updated successfully |
| `metadata` | json | Updated presentation metadata including ID, title, and URL |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `title` | string | The presentation title |
|   ↳ `mimeType` | string | The mime type of the presentation |
|   ↳ `url` | string | URL to open the presentation |

### `google_slides_create`

Create a new Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `title` | string | Да | The title of the presentation to create |
| `content` | string | Нет | The content to add to the first slide |
| `folderSelector` | string | Нет | Google Drive folder ID to create the presentation in \(e.g., 1ABCxyz...\) |
| `folderId` | string | Нет | The ID of the folder to create the presentation in \(internal use\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `metadata` | json | Created presentation metadata including ID, title, and URL |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `title` | string | The presentation title |
|   ↳ `mimeType` | string | The mime type of the presentation |
|   ↳ `url` | string | URL to open the presentation |

### `google_slides_replace_all_text`

Find and replace all occurrences of text throughout a Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `findText` | string | Да | The text to find \(e.g., \{\{placeholder\}\}\) |
| `replaceText` | string | Да | The text to replace with |
| `matchCase` | boolean | Нет | Whether the search should be case-sensitive \(default: true\) |
| `pageObjectIds` | string | Нет | Comma-separated list of slide object IDs to limit replacements to specific slides \(leave empty for all slides\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `occurrencesChanged` | number | Number of text occurrences that were replaced |
| `metadata` | json | Operation metadata including presentation ID and URL |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `findText` | string | The text that was searched for |
|   ↳ `replaceText` | string | The text that replaced the matches |
|   ↳ `url` | string | URL to open the presentation |

### `google_slides_add_slide`

Add a new slide to a Google Slides presentation with a specified layout

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `layout` | string | Нет | The predefined layout for the slide \(BLANK, TITLE, TITLE_AND_BODY, TITLE_ONLY, SECTION_HEADER, etc.\). Defaults to BLANK. |
| `insertionIndex` | number | Нет | The optional zero-based index indicating where to insert the slide. If not specified, the slide is added at the end. |
| `placeholderIdMappings` | string | Нет | JSON array of placeholder mappings to assign custom object IDs to placeholders. Format: \[\{"layoutPlaceholder":\{"type":"TITLE"\},"objectId":"custom_title_id"\}\] |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `slideId` | string | The object ID of the newly created slide |
| `metadata` | json | Operation metadata including presentation ID, layout, and URL |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `layout` | string | The layout used for the new slide |
|   ↳ `insertionIndex` | number | The zero-based index where the slide was inserted |
|   ↳ `url` | string | URL to open the presentation |

### `google_slides_add_image`

Insert an image into a specific slide in a Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `pageObjectId` | string | Да | The object ID of the slide/page to add the image to |
| `imageUrl` | string | Да | The publicly accessible URL of the image \(must be PNG, JPEG, or GIF, max 50MB\) |
| `width` | number | Нет | Width of the image in points \(default: 300\) |
| `height` | number | Нет | Height of the image in points \(default: 200\) |
| `positionX` | number | Нет | X position from the left edge in points \(default: 100\) |
| `positionY` | number | Нет | Y position from the top edge in points \(default: 100\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `imageId` | string | The object ID of the newly created image |
| `metadata` | json | Operation metadata including presentation ID and image URL |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `pageObjectId` | string | The page object ID where the image was inserted |
|   ↳ `imageUrl` | string | The source image URL |
|   ↳ `url` | string | URL to open the presentation |

### `google_slides_get_thumbnail`

Generate a thumbnail image of a specific slide in a Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `pageObjectId` | string | Да | The object ID of the slide/page to get a thumbnail for |
| `thumbnailSize` | string | Нет | The size of the thumbnail: SMALL \(200px\), MEDIUM \(800px\), or LARGE \(1600px\). Defaults to MEDIUM. |
| `mimeType` | string | Нет | The MIME type of the thumbnail image: PNG. Defaults to PNG. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `contentUrl` | string | URL to the thumbnail image \(valid for 30 minutes\) |
| `width` | number | Width of the thumbnail in pixels |
| `height` | number | Height of the thumbnail in pixels |
| `metadata` | json | Operation metadata including presentation ID and page object ID |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `pageObjectId` | string | The page object ID for the thumbnail |
|   ↳ `thumbnailSize` | string | The requested thumbnail size |
|   ↳ `mimeType` | string | The thumbnail MIME type |

### `google_slides_get_page`

Get detailed information about a specific slide/page in a Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `pageObjectId` | string | Да | The object ID of the slide/page to retrieve |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `objectId` | string | The object ID of the page |
| `pageType` | string | The type of page \(SLIDE, MASTER, LAYOUT, NOTES, NOTES_MASTER\) |
| `pageElements` | array | Array of page elements \(shapes, images, tables, etc.\) on this page |
| `slideProperties` | object | Properties specific to slides \(layout, master, notes\) |
|   ↳ `layoutObjectId` | string | Object ID of the layout this slide is based on |
|   ↳ `masterObjectId` | string | Object ID of the master this slide is based on |
|   ↳ `notesPage` | json | The notes page associated with the slide |
| `metadata` | object | Operation metadata including presentation ID and URL |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_delete_object`

Delete a page element (shape, image, table, etc.) or an entire slide from a Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | The object ID of the element or slide to delete |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `deleted` | boolean | Whether the object was successfully deleted |
| `objectId` | string | The object ID that was deleted |
| `metadata` | object | Operation metadata including presentation ID and URL |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_duplicate_object`

Duplicate an object (slide, shape, image, table, etc.) in a Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | The object ID of the element or slide to duplicate |
| `objectIds` | string | Нет | Optional JSON object mapping source object IDs \(within the slide being duplicated\) to new object IDs for the duplicates. Format: \{"sourceId1":"newId1","sourceId2":"newId2"\} |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `duplicatedObjectId` | string | The object ID of the newly created duplicate |
| `metadata` | object | Operation metadata including presentation ID and source object ID |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `sourceObjectId` | string | The original object ID that was duplicated |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_slides_position`

Move one or more slides to a new position in a Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `slideObjectIds` | string | Да | Comma-separated list of slide object IDs to move. The slides will maintain their relative order. |
| `insertionIndex` | number | Да | The zero-based index where the slides should be moved. All slides with indices greater than or equal to this will be shifted right. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `moved` | boolean | Whether the slides were successfully moved |
| `slideObjectIds` | array | The slide object IDs that were moved |
| `insertionIndex` | number | The index where the slides were moved to |
| `metadata` | object | Operation metadata including presentation ID and URL |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_create_table`

Create a new table on a slide in a Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `pageObjectId` | string | Да | The object ID of the slide/page to add the table to |
| `rows` | number | Да | Number of rows in the table \(minimum 1\) |
| `columns` | number | Да | Number of columns in the table \(minimum 1\) |
| `width` | number | Нет | Width of the table in points \(default: 400\) |
| `height` | number | Нет | Height of the table in points \(default: 200\) |
| `positionX` | number | Нет | X position from the left edge in points \(default: 100\) |
| `positionY` | number | Нет | Y position from the top edge in points \(default: 100\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `tableId` | string | The object ID of the newly created table |
| `rows` | number | Number of rows in the table |
| `columns` | number | Number of columns in the table |
| `metadata` | object | Operation metadata including presentation ID and page object ID |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `pageObjectId` | string | The page object ID where the table was created |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_create_shape`

Create a shape (rectangle, ellipse, text box, arrow, etc.) on a slide in a Google Slides presentation

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `pageObjectId` | string | Да | The object ID of the slide/page to add the shape to |
| `shapeType` | string | Да | The type of shape to create. Common types: TEXT_BOX, RECTANGLE, ROUND_RECTANGLE, ELLIPSE, TRIANGLE, DIAMOND, STAR_5, ARROW_EAST, HEART, CLOUD |
| `width` | number | Нет | Width of the shape in points \(default: 200\) |
| `height` | number | Нет | Height of the shape in points \(default: 100\) |
| `positionX` | number | Нет | X position from the left edge in points \(default: 100\) |
| `positionY` | number | Нет | Y position from the top edge in points \(default: 100\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `shapeId` | string | The object ID of the newly created shape |
| `shapeType` | string | The type of shape that was created |
| `metadata` | object | Operation metadata including presentation ID and page object ID |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `pageObjectId` | string | The page object ID where the shape was created |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_insert_text`

Insert text into a shape or table cell in a Google Slides presentation. Use this to add text to text boxes, shapes, or table cells.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | The object ID of the shape or table cell to insert text into. For table cells, use the cell object ID. |
| `text` | string | Да | The text to insert |
| `insertionIndex` | number | Нет | The zero-based index at which to insert the text. If not specified, text is inserted at the beginning \(index 0\). |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `inserted` | boolean | Whether the text was successfully inserted |
| `objectId` | string | The object ID where text was inserted |
| `text` | string | The text that was inserted |
| `metadata` | object | Operation metadata including presentation ID and URL |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_text_style`

Update the styling of text in a shape or table cell (bold, italic, font family, font size, foreground/background color, link, etc.). Only the fields you set are applied.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the shape or table containing the text |
| `rowIndex` | number | Нет | When targeting a table cell, the zero-based row index |
| `columnIndex` | number | Нет | When targeting a table cell, the zero-based column index |
| `rangeType` | string | Нет | Range to style: ALL \(default\), FROM_START_INDEX, or FIXED_RANGE |
| `startIndex` | number | Нет | Start index for FROM_START_INDEX or FIXED_RANGE |
| `endIndex` | number | Нет | End index for FIXED_RANGE |
| `bold` | boolean | Нет | Whether the text is bold |
| `italic` | boolean | Нет | Whether the text is italic |
| `underline` | boolean | Нет | Whether the text is underlined |
| `strikethrough` | boolean | Нет | Whether the text has strikethrough |
| `smallCaps` | boolean | Нет | Whether the text is rendered in small caps |
| `fontFamily` | string | Нет | Font family name \(must be a font available to Google Slides\) |
| `fontSize` | number | Нет | Font size in points |
| `foregroundColor` | string | Нет | Text color as hex \(e.g. #1A73E8\) |
| `backgroundColor` | string | Нет | Text background color as hex \(e.g. #FFF8E1\) |
| `linkUrl` | string | Нет | Convert the range to a hyperlink with this URL |
| `baselineOffset` | string | Нет | Baseline offset: NONE, SUPERSCRIPT, or SUBSCRIPT |
| `styleJson` | string | Нет | Advanced: raw TextStyle JSON merged with the simple fields above \(overrides them on conflict\) |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, the mask is computed from the fields you provided \(or "*" when styleJson is used without explicit fields\). |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the text style was updated |
| `objectId` | string | The object whose text was styled |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_paragraph_style`

Update paragraph styling — alignment, line spacing, indents, space above/below — for text in a shape or table cell.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the shape or table containing the text |
| `rowIndex` | number | Нет | When targeting a table cell, the zero-based row index |
| `columnIndex` | number | Нет | When targeting a table cell, the zero-based column index |
| `rangeType` | string | Нет | Range to style: ALL \(default\), FROM_START_INDEX, or FIXED_RANGE |
| `startIndex` | number | Нет | Start index for FROM_START_INDEX or FIXED_RANGE |
| `endIndex` | number | Нет | End index for FIXED_RANGE |
| `alignment` | string | Нет | Text alignment: START, CENTER, END, or JUSTIFIED |
| `lineSpacing` | number | Нет | Line spacing as a percentage \(100 = single, 200 = double\) |
| `indentStart` | number | Нет | Start-edge indent in points |
| `indentEnd` | number | Нет | End-edge indent in points |
| `indentFirstLine` | number | Нет | First-line indent in points |
| `spaceAbove` | number | Нет | Space above the paragraph in points |
| `spaceBelow` | number | Нет | Space below the paragraph in points |
| `direction` | string | Нет | Text direction: LEFT_TO_RIGHT or RIGHT_TO_LEFT |
| `spacingMode` | string | Нет | Spacing mode: NEVER_COLLAPSE or COLLAPSE_LISTS |
| `styleJson` | string | Нет | Advanced: raw ParagraphStyle JSON merged with the simple fields above |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, computed from provided fields. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the paragraph style was updated |
| `objectId` | string | The object whose paragraph was styled |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_delete_text`

Delete text from a shape or table cell. Use range type ALL to clear all text, or FIXED_RANGE / FROM_START_INDEX to delete a specific span.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the shape or table containing the text |
| `rowIndex` | number | Нет | When targeting a table cell, the zero-based row index |
| `columnIndex` | number | Нет | When targeting a table cell, the zero-based column index |
| `rangeType` | string | Нет | Range to delete: ALL \(default\), FROM_START_INDEX, or FIXED_RANGE |
| `startIndex` | number | Нет | Start index for FROM_START_INDEX or FIXED_RANGE |
| `endIndex` | number | Нет | End index for FIXED_RANGE |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `deleted` | boolean | Whether the text was deleted |
| `objectId` | string | The object whose text was deleted |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_create_paragraph_bullets`

Convert paragraphs in a shape or table cell into a bulleted or numbered list using a Google Slides bullet preset.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the shape or table containing the text |
| `rowIndex` | number | Нет | When targeting a table cell, the zero-based row index |
| `columnIndex` | number | Нет | When targeting a table cell, the zero-based column index |
| `rangeType` | string | Нет | Range to apply bullets to: ALL \(default\), FROM_START_INDEX, or FIXED_RANGE |
| `startIndex` | number | Нет | Start index for FROM_START_INDEX or FIXED_RANGE |
| `endIndex` | number | Нет | End index for FIXED_RANGE |
| `bulletPreset` | string | Нет | Bullet preset \(e.g. BULLET_DISC_CIRCLE_SQUARE, BULLET_ARROW_DIAMOND_DISC, NUMBERED_DIGIT_ALPHA_ROMAN, NUMBERED_DIGIT_ALPHA_ROMAN_PARENS, NUMBERED_DIGIT_NESTED\). Defaults to BULLET_DISC_CIRCLE_SQUARE. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `created` | boolean | Whether bullets were created |
| `objectId` | string | The object where bullets were created |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_delete_paragraph_bullets`

Remove bullets/numbering from paragraphs in a shape or table cell.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the shape or table containing the text |
| `rowIndex` | number | Нет | When targeting a table cell, the zero-based row index |
| `columnIndex` | number | Нет | When targeting a table cell, the zero-based column index |
| `rangeType` | string | Нет | Range to clear bullets from: ALL \(default\), FROM_START_INDEX, or FIXED_RANGE |
| `startIndex` | number | Нет | Start index for FROM_START_INDEX or FIXED_RANGE |
| `endIndex` | number | Нет | End index for FIXED_RANGE |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `deleted` | boolean | Whether bullets were deleted |
| `objectId` | string | The object whose bullets were deleted |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_replace_all_shapes_with_image`

Find every shape whose text matches the given token (e.g. \{\{cover-image\}\}) and replace it with an image, preserving the shape's position and bounds.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `imageUrl` | string | Да | Publicly fetchable image URL \(PNG, JPEG, or GIF; max 50 MB and accessible to Google's servers\) |
| `findText` | string | Да | Text content of shapes to replace \(e.g. \{\{cover-image\}\}\) |
| `matchCase` | boolean | Нет | Case-sensitive match \(default: true\) |
| `imageReplaceMethod` | string | Нет | How the image fits the shape: CENTER_INSIDE \(preserve aspect, fit inside\) or CENTER_CROP \(fill, crop overflow\). Default: CENTER_INSIDE. |
| `pageObjectIds` | string | Нет | Comma-separated slide IDs to limit replacement to specific slides |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `occurrencesChanged` | number | Number of shapes that were replaced with the image |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |
|   ↳ `imageUrl` | string | The image URL inserted |
|   ↳ `findText` | string | The matched text token |

### `google_slides_replace_image`

Replace the source of an existing image with a new image URL, preserving the image's position, size, and properties.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `imageObjectId` | string | Да | Object ID of the existing image to replace |
| `imageUrl` | string | Да | New publicly fetchable image URL \(PNG, JPEG, or GIF, max 50 MB\) |
| `imageReplaceMethod` | string | Нет | CENTER_INSIDE \(preserve aspect\) or CENTER_CROP \(fill, crop overflow\). Default: CENTER_INSIDE. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `replaced` | boolean | Whether the image was replaced |
| `imageObjectId` | string | The image object that was replaced |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |
|   ↳ `imageUrl` | string | The new image URL |

### `google_slides_update_image_properties`

Update image properties — brightness, contrast, transparency, crop, outline, link — on an existing image.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the image to update |
| `brightness` | number | Нет | Brightness adjustment between -1.0 and 1.0 |
| `contrast` | number | Нет | Contrast adjustment between -1.0 and 1.0 |
| `transparency` | number | Нет | Transparency between 0.0 \(opaque\) and 1.0 \(fully transparent\) |
| `linkUrl` | string | Нет | Make the image a hyperlink to this URL |
| `outlineColor` | string | Нет | Outline color as hex \(e.g. #1A73E8\) |
| `outlineWeight` | number | Нет | Outline weight in points |
| `outlineDashStyle` | string | Нет | Outline dash style: SOLID, DOT, DASH, DASH_DOT, LONG_DASH, LONG_DASH_DOT |
| `cropLeftOffset` | number | Нет | Crop offset from left edge \(0.0 to 1.0\) |
| `cropRightOffset` | number | Нет | Crop offset from right edge \(0.0 to 1.0\) |
| `cropTopOffset` | number | Нет | Crop offset from top edge \(0.0 to 1.0\) |
| `cropBottomOffset` | number | Нет | Crop offset from bottom edge \(0.0 to 1.0\) |
| `cropAngle` | number | Нет | Crop rotation angle in radians \(clockwise\) |
| `propertiesJson` | string | Нет | Advanced: raw ImageProperties JSON merged with the simple fields above |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, computed from provided fields. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the image properties were updated |
| `objectId` | string | The image object updated |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_shape_properties`

Update a shape's appearance — background fill color, outline, link, content alignment, autofit. Pass only the properties you want to change.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the shape to update |
| `fillColor` | string | Нет | Solid background fill color as hex \(e.g. #FF6F61\) |
| `fillAlpha` | number | Нет | Fill opacity between 0.0 \(transparent\) and 1.0 \(opaque\) |
| `fillUnset` | boolean | Нет | When true, removes any fill so the shape inherits its layout/master fill |
| `outlineColor` | string | Нет | Outline color as hex |
| `outlineWeight` | number | Нет | Outline weight in points |
| `outlineDashStyle` | string | Нет | Outline dash style: SOLID, DOT, DASH, DASH_DOT, LONG_DASH, LONG_DASH_DOT |
| `outlineUnset` | boolean | Нет | When true, removes any outline so the shape inherits its layout/master outline |
| `linkUrl` | string | Нет | Make the shape a hyperlink to this URL |
| `contentAlignment` | string | Нет | Vertical alignment of shape contents: TOP, MIDDLE, or BOTTOM |
| `autofitType` | string | Нет | Autofit behavior: NONE, TEXT_AUTOFIT, or SHAPE_AUTOFIT |
| `propertiesJson` | string | Нет | Advanced: raw ShapeProperties JSON merged with the simple fields above |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, computed from provided fields. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the shape properties were updated |
| `objectId` | string | The shape object updated |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_page_properties`

Update slide/page background — solid color or stretched picture — and other page properties.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the slide/page to update |
| `backgroundColor` | string | Нет | Solid background color as hex \(e.g. #0B1F3A\) |
| `backgroundAlpha` | number | Нет | Background fill opacity between 0.0 and 1.0 |
| `backgroundImageUrl` | string | Нет | Publicly fetchable image URL to use as a stretched picture background |
| `backgroundUnset` | boolean | Нет | When true, removes the background so the slide inherits its layout background |
| `propertiesJson` | string | Нет | Advanced: raw PageProperties JSON merged with the simple fields above |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, computed from provided fields. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the page properties were updated |
| `objectId` | string | The page object updated |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_slide_properties`

Update slide-specific properties such as whether the slide is skipped during presentation.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the slide to update |
| `isSkipped` | boolean | Нет | Whether the slide is skipped in presentation mode |
| `propertiesJson` | string | Нет | Advanced: raw SlideProperties JSON merged with the simple fields above |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, computed from provided fields. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the slide properties were updated |
| `objectId` | string | The slide object updated |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_page_element_alt_text`

Set the accessibility title and/or description (alt text) of a page element such as an image, shape, or group.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the page element |
| `title` | string | Нет | Accessibility title for the element |
| `description` | string | Нет | Accessibility description \(alt text\) for the element |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether alt text was updated |
| `objectId` | string | The element updated |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_page_element_transform`

Move, resize, scale, or shear a page element. Translate is specified in points; applyMode controls whether the transform is absolute (default) or relative (multiplied with the current transform).

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the page element to transform |
| `scaleX` | number | Нет | Horizontal scale factor \(default 1\) |
| `scaleY` | number | Нет | Vertical scale factor \(default 1\) |
| `shearX` | number | Нет | Horizontal shear factor \(default 0\) |
| `shearY` | number | Нет | Vertical shear factor \(default 0\) |
| `translateX` | number | Нет | X position in points \(absolute\) or delta \(relative\) |
| `translateY` | number | Нет | Y position in points \(absolute\) or delta \(relative\) |
| `applyMode` | string | Нет | ABSOLUTE replaces the current transform; RELATIVE multiplies with it. Default ABSOLUTE. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the transform was updated |
| `objectId` | string | The element transformed |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_page_elements_z_order`

Bring elements to front, send to back, or step them one layer forward/backward.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectIds` | string | Да | Comma-separated object IDs of the elements to reorder |
| `operation` | string | Да | BRING_TO_FRONT, BRING_FORWARD, SEND_BACKWARD, or SEND_TO_BACK |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `reordered` | boolean | Whether the z-order was changed |
| `objectIds` | array | Elements reordered |
| `operation` | string | Operation applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_group_objects`

Group two or more page elements on the same slide into a single object group.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `childrenObjectIds` | string | Да | Comma-separated object IDs of the elements to group \(must be on the same slide\) |
| `groupObjectId` | string | Нет | Optional object ID to assign to the new group |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `grouped` | boolean | Whether the objects were grouped |
| `groupObjectId` | string | Object ID of the new group |
| `childrenObjectIds` | array | IDs of the grouped children |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_ungroup_objects`

Ungroup one or more object groups, releasing their children back to the slide.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectIds` | string | Да | Comma-separated object IDs of the groups to ungroup |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `ungrouped` | boolean | Whether the objects were ungrouped |
| `objectIds` | array | Group IDs that were ungrouped |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_create_line`

Create a line or connector on a slide.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `pageObjectId` | string | Да | Object ID of the slide to add the line to |
| `lineCategory` | string | Нет | STRAIGHT \(default\), BENT, or CURVED |
| `width` | number | Нет | Line width in points \(default 200\) |
| `height` | number | Нет | Line height in points \(default 0 — horizontal line\) |
| `positionX` | number | Нет | X position in points \(default 100\) |
| `positionY` | number | Нет | Y position in points \(default 100\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `lineId` | string | Object ID of the new line |
| `lineCategory` | string | Line category created |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `pageObjectId` | string | The slide ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_line_properties`

Update line appearance — color, weight, dash style, arrows, link.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the line |
| `lineColor` | string | Нет | Line color as hex |
| `lineWeight` | number | Нет | Line weight in points |
| `dashStyle` | string | Нет | Dash style: SOLID, DOT, DASH, DASH_DOT, LONG_DASH, LONG_DASH_DOT |
| `startArrow` | string | Нет | Start arrow style: NONE, STEALTH_ARROW, FILL_ARROW, FILL_CIRCLE, FILL_SQUARE, FILL_DIAMOND, OPEN_ARROW, OPEN_CIRCLE, OPEN_SQUARE, OPEN_DIAMOND |
| `endArrow` | string | Нет | End arrow style \(same values as startArrow\) |
| `linkUrl` | string | Нет | Hyperlink URL |
| `propertiesJson` | string | Нет | Advanced: raw LineProperties JSON merged with the simple fields above |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, computed from provided fields. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the line properties were updated |
| `objectId` | string | The line object updated |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_line_category`

Change a connector line's category (STRAIGHT, BENT, or CURVED).

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the connector line |
| `lineCategory` | string | Да | New line category: STRAIGHT, BENT, or CURVED |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the line category was updated |
| `objectId` | string | The line object updated |
| `lineCategory` | string | New line category |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_reroute_line`

Reroute a connector line so it efficiently connects its endpoint shapes — useful after moving the shapes the line connects.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the connector line to reroute |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `rerouted` | boolean | Whether the line was rerouted |
| `objectId` | string | The line object rerouted |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_insert_table_rows`

Insert one or more rows into a table, above or below a reference cell.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `tableObjectId` | string | Да | Object ID of the table |
| `rowIndex` | number | Да | Zero-based row index of the reference cell |
| `columnIndex` | number | Да | Zero-based column index of the reference cell |
| `number` | number | Да | Number of rows to insert \(minimum 1\) |
| `insertBelow` | boolean | Нет | Insert below the reference row instead of above \(default false\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `inserted` | boolean | Whether rows were inserted |
| `tableObjectId` | string | The table updated |
| `number` | number | Number of rows inserted |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_insert_table_columns`

Insert one or more columns into a table, left or right of a reference cell.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `tableObjectId` | string | Да | Object ID of the table |
| `rowIndex` | number | Да | Zero-based row index of the reference cell |
| `columnIndex` | number | Да | Zero-based column index of the reference cell |
| `number` | number | Да | Number of columns to insert \(minimum 1\) |
| `insertRight` | boolean | Нет | Insert to the right of the reference column instead of left \(default false\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `inserted` | boolean | Whether columns were inserted |
| `tableObjectId` | string | The table updated |
| `number` | number | Number of columns inserted |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_delete_table_row`

Delete the row containing the reference cell from a table.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `tableObjectId` | string | Да | Object ID of the table |
| `rowIndex` | number | Да | Zero-based row index identifying the row to delete |
| `columnIndex` | number | Да | Zero-based column index of any cell in the row |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `deleted` | boolean | Whether the row was deleted |
| `tableObjectId` | string | The table updated |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_delete_table_column`

Delete the column containing the reference cell from a table.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `tableObjectId` | string | Да | Object ID of the table |
| `rowIndex` | number | Да | Zero-based row index of any cell in the column |
| `columnIndex` | number | Да | Zero-based column index identifying the column to delete |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `deleted` | boolean | Whether the column was deleted |
| `tableObjectId` | string | The table updated |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_merge_table_cells`

Merge a rectangular range of table cells into a single cell. The range starts at (rowIndex, columnIndex) and covers rowSpan × columnSpan cells.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the table |
| `rowIndex` | number | Да | Zero-based row index of the top-left cell |
| `columnIndex` | number | Да | Zero-based column index of the top-left cell |
| `rowSpan` | number | Да | Number of rows to merge \(minimum 1\) |
| `columnSpan` | number | Да | Number of columns to merge \(minimum 1\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `merged` | boolean | Whether the cells were merged |
| `objectId` | string | The table updated |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_unmerge_table_cells`

Unmerge any merged cells within the given table range.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the table |
| `rowIndex` | number | Да | Zero-based row index of the top-left cell of the range |
| `columnIndex` | number | Да | Zero-based column index of the top-left cell of the range |
| `rowSpan` | number | Да | Number of rows in the range \(minimum 1\) |
| `columnSpan` | number | Да | Number of columns in the range \(minimum 1\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `unmerged` | boolean | Whether the cells were unmerged |
| `objectId` | string | The table updated |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_table_cell_properties`

Update background fill and content alignment for a range of table cells.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the table |
| `rowIndex` | number | Да | Zero-based row index of the top-left cell of the range |
| `columnIndex` | number | Да | Zero-based column index of the top-left cell of the range |
| `rowSpan` | number | Да | Number of rows in the range \(minimum 1\) |
| `columnSpan` | number | Да | Number of columns in the range \(minimum 1\) |
| `backgroundColor` | string | Нет | Cell background color as hex \(e.g. #F1F3F4\) |
| `backgroundAlpha` | number | Нет | Background fill opacity between 0.0 and 1.0 |
| `contentAlignment` | string | Нет | Vertical alignment of cell content: TOP, MIDDLE, or BOTTOM |
| `propertiesJson` | string | Нет | Advanced: raw TableCellProperties JSON merged with the simple fields above |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, computed from provided fields. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the cell properties were updated |
| `objectId` | string | The table updated |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_table_border_properties`

Update border color, weight, and dash style for a position (e.g. ALL, INNER, OUTER) in a table range.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the table |
| `rowIndex` | number | Да | Zero-based row index of the top-left cell of the range |
| `columnIndex` | number | Да | Zero-based column index of the top-left cell of the range |
| `rowSpan` | number | Да | Number of rows in the range \(minimum 1\) |
| `columnSpan` | number | Да | Number of columns in the range \(minimum 1\) |
| `borderPosition` | string | Нет | Which borders to update: ALL \(default\), BOTTOM, INNER, INNER_HORIZONTAL, INNER_VERTICAL, LEFT, OUTER, RIGHT, TOP |
| `borderColor` | string | Нет | Border color as hex |
| `borderWeight` | number | Нет | Border weight in points |
| `dashStyle` | string | Нет | Dash style: SOLID, DOT, DASH, DASH_DOT, LONG_DASH, LONG_DASH_DOT |
| `propertiesJson` | string | Нет | Advanced: raw TableBorderProperties JSON merged with the simple fields above |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, computed from provided fields. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the border properties were updated |
| `objectId` | string | The table updated |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_table_column_properties`

Update column widths and other column-level table properties.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the table |
| `columnIndices` | string | Да | Comma-separated zero-based column indices to update \(e.g. "0,2,3"\) |
| `columnWidth` | number | Нет | Column width in points |
| `propertiesJson` | string | Нет | Advanced: raw TableColumnProperties JSON merged with the simple fields above |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, computed from provided fields. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the column properties were updated |
| `objectId` | string | The table updated |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_table_row_properties`

Update minimum row heights and other row-level table properties.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the table |
| `rowIndices` | string | Да | Comma-separated zero-based row indices to update \(e.g. "0,2,3"\) |
| `minRowHeight` | number | Нет | Minimum row height in points |
| `propertiesJson` | string | Нет | Advanced: raw TableRowProperties JSON merged with the simple fields above |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, computed from provided fields. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the row properties were updated |
| `objectId` | string | The table updated |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_create_sheets_chart`

Embed a chart from a Google Sheets spreadsheet onto a slide. LINKED charts can be refreshed; NOT_LINKED_IMAGE inserts a static image of the chart.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `pageObjectId` | string | Да | Object ID of the slide to add the chart to |
| `spreadsheetId` | string | Да | Google Sheets spreadsheet ID containing the chart |
| `chartId` | number | Да | Numeric chart ID within the spreadsheet |
| `linkingMode` | string | Нет | LINKED \(default\) or NOT_LINKED_IMAGE |
| `width` | number | Нет | Width in points \(default 400\) |
| `height` | number | Нет | Height in points \(default 300\) |
| `positionX` | number | Нет | X position in points \(default 100\) |
| `positionY` | number | Нет | Y position in points \(default 100\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `chartObjectId` | string | Object ID of the inserted chart |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `pageObjectId` | string | The slide ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_refresh_sheets_chart`

Refresh an embedded linked Sheets chart so it reflects the latest spreadsheet data.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the embedded chart to refresh |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `refreshed` | boolean | Whether the chart was refreshed |
| `objectId` | string | The chart object refreshed |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_replace_all_shapes_with_sheets_chart`

Find every shape matching a text token (e.g. \{\{revenue-chart\}\}) and replace each with the same embedded Sheets chart, preserving the shape's position and bounds.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `spreadsheetId` | string | Да | Google Sheets spreadsheet ID containing the chart |
| `chartId` | number | Да | Numeric chart ID within the spreadsheet |
| `findText` | string | Да | Text content of shapes to replace \(e.g. \{\{revenue-chart\}\}\) |
| `matchCase` | boolean | Нет | Case-sensitive match \(default true\) |
| `linkingMode` | string | Нет | LINKED \(default\) or NOT_LINKED_IMAGE |
| `pageObjectIds` | string | Нет | Comma-separated slide IDs to limit replacement to specific slides |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `occurrencesChanged` | number | Number of shapes replaced with the chart |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |
|   ↳ `findText` | string | The matched text token |
|   ↳ `spreadsheetId` | string | Source spreadsheet ID |
|   ↳ `chartId` | number | Source chart ID |

### `google_slides_create_video`

Embed a YouTube or Google Drive video on a slide.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `pageObjectId` | string | Да | Object ID of the slide to add the video to |
| `source` | string | Да | YOUTUBE or DRIVE |
| `videoId` | string | Да | YouTube video ID or Drive file ID |
| `width` | number | Нет | Width in points \(default 400\) |
| `height` | number | Нет | Height in points \(default 225\) |
| `positionX` | number | Нет | X position in points \(default 100\) |
| `positionY` | number | Нет | Y position in points \(default 100\) |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `videoObjectId` | string | Object ID of the inserted video |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `pageObjectId` | string | The slide ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_update_video_properties`

Update video playback options (autoPlay, mute, start/end) and outline.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `objectId` | string | Да | Object ID of the video |
| `autoPlay` | boolean | Нет | Play the video automatically when the slide is shown |
| `mute` | boolean | Нет | Mute the video |
| `start` | number | Нет | Playback start time in seconds |
| `end` | number | Нет | Playback end time in seconds |
| `outlineColor` | string | Нет | Outline color as hex |
| `outlineWeight` | number | Нет | Outline weight in points |
| `outlineDashStyle` | string | Нет | Outline dash style |
| `propertiesJson` | string | Нет | Advanced: raw VideoProperties JSON merged with the simple fields above |
| `fields` | string | Нет | Advanced: explicit FieldMask. If omitted, computed from provided fields. |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `updated` | boolean | Whether the video properties were updated |
| `objectId` | string | The video object updated |
| `fields` | string | FieldMask applied |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |

### `google_slides_batch_update`

Run a raw Slides API batchUpdate with a list of Request objects. Use this when the higher-level tools do not cover an operation, or to bundle multiple operations into a single atomic batch (all-or-nothing).

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `requests` | string | Да | JSON array of Slides API Request objects. Example: \[\{"replaceAllText":\{"containsText":\{"text":"\{\{title\}\}"\},"replaceText":"Q3 Review"\}\}, \{"updatePageProperties":\{"objectId":"slide_1","pageProperties":\{"pageBackgroundFill":\{"solidFill":\{"color":\{"rgbColor":\{"red":0.043,"green":0.122,"blue":0.231\}\}\}\}\},"fields":"pageBackgroundFill"\}\}\] |
| `writeControl` | string | Нет | Optional JSON WriteControl object for optimistic concurrency, e.g. \{"requiredRevisionId":"..."\} |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `replies` | array | Array of reply objects, one per request \(parallel-indexed\) |
| `writeControl` | json | WriteControl returned by the server \(revision tracking\) |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |
|   ↳ `requestCount` | number | Number of replies returned |

### `google_slides_copy_presentation`

Copy a template presentation in Drive to a new file. Use this before merging data so the original template is never modified.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `sourcePresentationId` | string | Да | Drive file ID of the source/template presentation |
| `title` | string | Нет | Title for the copy. Defaults to "Copy of &lt;source title&gt;". |
| `folderId` | string | Нет | Drive folder ID where the copy should be placed |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `presentationId` | string | ID of the new copied presentation |
| `title` | string | Title of the new presentation |
| `metadata` | object | Operation metadata |
|   ↳ `sourcePresentationId` | string | Source/template presentation ID |
|   ↳ `presentationId` | string | New presentation ID |
|   ↳ `title` | string | New presentation title |
|   ↳ `mimeType` | string | MIME type of the presentation |
|   ↳ `url` | string | URL to the new presentation |

### `google_slides_export_presentation`

Export a presentation to PDF, PPTX, ODP, TXT, PNG, JPEG, or SVG via the Drive export endpoint. Stores the exported file as an execution file when execution context is available.

#### Входные параметры

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `presentationId` | string | Да | Google Slides presentation ID |
| `exportFormat` | string | Нет | Format: PDF \(default\), PPTX, ODP, TXT, PNG, JPEG, or SVG |

#### Выходные параметры

| Параметр | Тип | Описание |
| --------- | ---- | ----------- |
| `file` | file | Stored exported presentation file |
| `contentBase64` | string | Deprecated legacy inline content. New exports return file. |
| `mimeType` | string | MIME type of the exported content |
| `sizeBytes` | number | Size of the exported content in bytes |
| `metadata` | object | Operation metadata |
|   ↳ `presentationId` | string | The presentation ID |
|   ↳ `url` | string | URL to the presentation |
|   ↳ `exportFormat` | string | Export format used |


