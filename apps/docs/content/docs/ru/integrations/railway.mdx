---
title: Railway
description: Управление проектами, сервисами, развёртываниями и переменными Railway
---

import { BlockInfoCard } from "@/components/ui/block-info-card"

<BlockInfoCard 
  type="railway"
  color="#000000"
/>

{/* MANUAL-CONTENT-START:intro */}
[Railway](https://railway.com/) is a cloud application platform for deploying, operating, and scaling services, databases, jobs, and production environments from a single project workspace. Teams use Railway to connect source repositories, manage environments, configure variables, trigger deployments, and monitor delivery across staging and production.

With Railway, you can:

- **Manage projects and environments**: Organize deployed services and inspect the environments attached to each project
- **Automate deployments**: Trigger new service deployments and inspect recent deployment status from workflows
- **Control runtime configuration**: Read and update environment variables for services or shared project environments
- **Connect infrastructure workflows**: Use project, service, and environment IDs from one step to drive release automation in later steps

In Sim, the Railway integration lets your agents work with Railway's public GraphQL API directly from workflows. You can list projects, fetch project services and environments, inspect deployments, deploy a service, and manage environment variables as part of CI/CD, operations, and release processes.
{/* MANUAL-CONTENT-END */}


## Инструкции по использованию

Интегрируйте Railway into workflows to list projects, manage services and environments, monitor deployments, trigger and roll back service deployments, and manage environment variables.



## Действия

### `railway_list_projects`

Получение списка Railway projects visible to the provided token

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `workspaceId` | string | Нет | Workspace ID to list projects from |
| `first` | number | Нет | Maximum number of projects to return |
| `after` | string | Нет | Cursor for pagination |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `projects` | array | Railway projects |
|   ↳ `id` | string | Project ID |
|   ↳ `name` | string | Project name |
|   ↳ `description` | string | Project description |
|   ↳ `createdAt` | string | Project creation timestamp |
|   ↳ `updatedAt` | string | Project update timestamp |
| `pageInfo` | object | Pagination information |
|   ↳ `hasNextPage` | boolean | Whether more projects are available |
|   ↳ `endCursor` | string | Cursor for the next page |
| `count` | number | Number of projects returned |

### `railway_get_project`

Получение a Railway project with its services and environments

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `projectId` | string | Да | Railway project ID |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `project` | object | Project with services and environments |
|   ↳ `id` | string | Project ID |
|   ↳ `name` | string | Project name |
|   ↳ `description` | string | Project description |
|   ↳ `createdAt` | string | Project creation timestamp |
|   ↳ `updatedAt` | string | Project update timestamp |
|   ↳ `services` | array | Project services |
|     ↳ `id` | string | Service ID |
|     ↳ `name` | string | Service name |
|     ↳ `icon` | string | Service icon |
|   ↳ `environments` | array | Project environments |
|     ↳ `id` | string | Environment ID |
|     ↳ `name` | string | Environment name |

### `railway_create_project`

Create a Railway project

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `name` | string | Да | Project name |
| `description` | string | Нет | Project description |
| `workspaceId` | string | Нет | Workspace ID to create the project in |
| `isPublic` | boolean | Нет | Указывает,  project should be publicly visible |
| `defaultEnvironmentName` | string | Нет | Name for the по умолчанию environment |
| `prDeploys` | boolean | Нет | Whether to enable pull request deploys |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `project` | object | Created project |
|   ↳ `id` | string | Project ID |
|   ↳ `name` | string | Project name |

### `railway_update_project`

Update a Railway project name or description

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `projectId` | string | Да | Railway project ID |
| `name` | string | Нет | Updated project name |
| `description` | string | Нет | Updated project description |
| `isPublic` | boolean | Нет | Указывает,  project should be publicly visible |
| `prDeploys` | boolean | Нет | Whether to enable pull request deploy environments |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `project` | object | Updated project |
|   ↳ `id` | string | Project ID |
|   ↳ `name` | string | Project name |
|   ↳ `description` | string | Project description |

### `railway_delete_project`

Удаление Railway project

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `projectId` | string | Да | Railway project ID |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Указывает,  project was deleted |

### `railway_transfer_project`

Transfer a Railway project to another workspace

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `projectId` | string | Да | Railway project ID |
| `workspaceId` | string | Да | Destination workspace ID |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Указывает,  project was transferred |

### `railway_list_project_members`

Получение списка members for a Railway project

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `projectId` | string | Да | Railway project ID |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `members` | array | Project members |
|   ↳ `id` | string | Member user ID |
|   ↳ `role` | string | Project role |
|   ↳ `name` | string | Member name |
|   ↳ `email` | string | Member email |
|   ↳ `avatar` | string | Member avatar URL |
| `count` | number | Number of members returned |

### `railway_create_environment`

Create a Railway project environment

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `projectId` | string | Да | Railway project ID |
| `name` | string | Да | Environment name |
| `sourceEnvironmentId` | string | Нет | Environment ID to clone from |
| `ephemeral` | boolean | Нет | Указывает,  environment is ephemeral |
| `skipInitialDeploys` | boolean | Нет | Whether to skip initial deploys for the environment |
| `stageInitialChanges` | boolean | Нет | Whether to stage initial changes instead of applying them immediately |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `environment` | object | Created environment |
|   ↳ `id` | string | Environment ID |
|   ↳ `name` | string | Environment name |

### `railway_delete_environment`

Удаление Railway project environment

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `environmentId` | string | Да | Railway environment ID |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Указывает,  environment was deleted |

### `railway_create_service`

Create a Railway service from a GitHub repo or Docker image

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `projectId` | string | Да | Railway project ID |
| `name` | string | Да | Service name |
| `repo` | string | Нет | GitHub repository in owner/name format to deploy from |
| `image` | string | Нет | Docker image to deploy, for example redis:7-alpine |
| `branch` | string | Нет | Git branch to deploy when using a repository source |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `service` | object | Created service |
|   ↳ `id` | string | Service ID |
|   ↳ `name` | string | Service name |

### `railway_delete_service`

Удаление Railway service and all of its deployments

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `serviceId` | string | Да | Railway service ID |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Указывает,  service was deleted |

### `railway_list_deployments`

Получение списка deployments for a Railway service in an environment

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `projectId` | string | Да | Railway project ID |
| `serviceId` | string | Да | Railway service ID |
| `environmentId` | string | Да | Railway environment ID |
| `first` | number | Нет | Maximum number of deployments to return |
| `after` | string | Нет | Cursor for pagination |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `deployments` | array | Service deployments |
|   ↳ `id` | string | Deployment ID |
|   ↳ `status` | string | Deployment status |
|   ↳ `createdAt` | string | Deployment creation timestamp |
|   ↳ `url` | string | Deployment URL |
|   ↳ `staticUrl` | string | Static deployment URL |
|   ↳ `canRollback` | boolean | Whether this deployment can be rolled back to |
|   ↳ `canRedeploy` | boolean | Whether this deployment can be redeployed |
| `count` | number | Number of deployments returned |
| `pageInfo` | object | Pagination information |
|   ↳ `hasNextPage` | boolean | Whether more deployments are available |
|   ↳ `endCursor` | string | Cursor for the next page |

### `railway_get_deployment`

Получение details for a single Railway deployment

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `deploymentId` | string | Да | Railway deployment ID |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `deployment` | object | Deployment details |
|   ↳ `id` | string | Deployment ID |
|   ↳ `status` | string | Deployment status |
|   ↳ `createdAt` | string | Deployment creation timestamp |
|   ↳ `url` | string | Deployment URL |
|   ↳ `staticUrl` | string | Static deployment URL |
|   ↳ `canRollback` | boolean | Указывает,  deployment can be rolled back to |
|   ↳ `canRedeploy` | boolean | Указывает,  deployment can be redeployed |

### `railway_deploy_service`

Trigger a deployment for a Railway service in an environment

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `serviceId` | string | Да | Railway service ID |
| `environmentId` | string | Да | Railway environment ID |
| `commitSha` | string | Нет | Specific Git commit SHA to deploy |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `deploymentId` | string | Created deployment ID |

### `railway_restart_deployment`

Restart a running Railway deployment without rebuilding

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `deploymentId` | string | Да | Railway deployment ID |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Указывает,  deployment was restarted |

### `railway_rollback_deployment`

Roll a Railway service back to a previous deployment

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `deploymentId` | string | Да | Railway deployment ID to roll back to \(must have canRollback\) |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Указывает,  rollback was triggered |

### `railway_get_deployment_logs`

Получение runtime logs for a Railway deployment

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `deploymentId` | string | Да | Railway deployment ID |
| `limit` | number | Нет | Maximum number of log lines to return |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `logs` | array | Deployment log entries |
|   ↳ `timestamp` | string | Log timestamp |
|   ↳ `message` | string | Log message |
|   ↳ `severity` | string | Log severity |
| `count` | number | Number of log entries returned |

### `railway_list_variables`

Получение списка Railway environment variables for a service or shared environment

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `projectId` | string | Да | Railway project ID |
| `environmentId` | string | Да | Railway environment ID |
| `serviceId` | string | Нет | Railway service ID. Omit for shared environment variables. |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `variables` | object | Variable names and values |
| `count` | number | Number of variables returned |

### `railway_upsert_variable`

Create or update a Railway environment variable

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `projectId` | string | Да | Railway project ID |
| `environmentId` | string | Да | Railway environment ID |
| `serviceId` | string | Нет | Railway service ID. Omit to create or update a shared variable. |
| `name` | string | Да | Variable name |
| `value` | string | Да | Variable value |
| `skipDeploys` | boolean | Нет | Whether to skip automatic redeploys after changing the variable |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Указывает,  variable was created or updated |

### `railway_delete_variable`

Удаление Railway environment variable

#### Вход

| Параметр | Тип | Обязательный | Описание |
| --------- | ---- | -------- | ----------- |
| `apiKey` | string | Да | Railway API token |
| `tokenType` | string | Нет | Railway token type. Use "account" for account, workspace, or OAuth tokens, or "project" for project tokens. |
| `projectId` | string | Да | Railway project ID |
| `environmentId` | string | Да | Railway environment ID |
| `name` | string | Да | Variable name to delete |
| `serviceId` | string | Нет | Railway service ID. Omit to delete a shared variable. |

#### Выход

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Указывает,  variable was deleted |


