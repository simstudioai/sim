---
title: Приём и извлечение данных из документов
description: Конвейер, который принимает входящие документы, извлекает нужные вам поля и записывает структурированные строки, с которыми можно работать.
---

import { VideoPlaceholder } from '@/components/ui/video-placeholder'
import { WhatYouWillLearn } from '@/components/ui/what-you-will-learn'
import { VideoChapters } from '@/components/ui/video-chapters'

<VideoPlaceholder eyebrow="Пример использования" title="Приём и извлечение данных из документов" className="mt-2" />

<div className="mt-8 grid grid-cols-1 items-start gap-10 lg:grid-cols-[1fr_300px]">
<div>

Превратите груду неструктурированных документов — счета, контракты, формы — в структурированные данные. Воркфлоу читает каждый файл, извлекает значимые поля и записывает их как строки, которые вы можете запрашивать и обрабатывать.

<WhatYouWillLearn
  items={[
    { title: 'Чтение реальных файлов', body: 'Примите PDF или изображение и извлеките его содержимое в воркфлоу.' },
    { title: 'Извлечение с помощью агента', body: 'Используйте структурированный вывод, чтобы агент возвращал чистые, предсказуемые поля.' },
    { title: 'Сохранение результата', body: 'Запишите каждое извлечение как строку в таблице для последующей работы.' },
  ]}
/>

</div>

<VideoChapters
  chapters={[
    { title: 'Проблема', time: '0:00' },
    { title: 'Загрузка файла', time: '0:35' },
    { title: 'Извлечение структурированных полей', time: '1:20' },
    { title: 'Запись строк в таблицу', time: '2:15' },
    { title: 'Запуск на пакете', time: '3:00' },
  ]}
/>

</div>

## Что вы построите

Поступает **файл**, блок **Agent** извлекает поля в [структурированный вывод](/academy/agents/intro), чтобы форма была предсказуемой, а блок **Table** записывает строку. Запустите это на пакете, и папка документов становится запрашиваемой таблицей.

Это объединяет [файлы](/academy/files/intro), [агентов](/academy/agents/intro) и [таблицы](/academy/tables/intro).
