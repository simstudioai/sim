---
title: Журналирование
description: Где находятся журналы запусков — консоль реального времени, страница журналов и что записывает каждая запись.
pageType: reference
---

import { Callout } from 'fumadocs-ui/components/callout'
import { Tab, Tabs } from 'fumadocs-ui/components/tabs'
import { Image } from '@/components/ui/image'

Каждый запуск рабочего процесса журналируется. Есть два места для их чтения: **Консоль** для запуска, за которым вы следите, и **Страница журналов** для всего остального.

## Консоль реального времени

Во время ручных или чат-запусков панель Консоль в редакторе показывает каждый блок по мере его выполнения — подсветка активного блока, выводы по мере их завершения, время выполнения каждого блока и статус успех/ошибка.

<div className="flex justify-center">
  <Image
    src="/static/logs/console.png"
    alt="Панель консоли реального времени"
    width={400}
    height={300}
    className="my-6"
  />
</div>

## Страница журналов

Каждый запуск от любого триггера — ручной, API, чат, расписание, вебхук — попадает на страницу журналов с фильтрацией по временному диапазону, статусу, типу триггера, папке и рабочему процессу, полнотекстовым поиском и **живым режимом**, который транслирует новые записи по мере их записи.

<div className="flex justify-center">
  <Image
    src="/static/logs/logs.png"
    alt="Страница журналов"
    width={600}
    height={400}
    className="my-6"
  />
</div>

## Детали журнала

Нажмите на любую запись, чтобы открыть боковую панель: временную шкалу запуска (начало/конец, общая длительность, время выполнения каждого блока) и поток данных каждого блока.

<div className="flex justify-center">
  <Image
    src="/static/logs/logs-sidebar.png"
    alt="Боковая панель деталей журнала"
    width={600}
    height={400}
    className="my-6"
  />
</div>

<Tabs items={['Output', 'Input']}>
  <Tab>
    Результат блока — структурированные данные в формате JSON, рендеринг markdown для ИИ-содержимого и кнопка копирования.
  </Tab>
  <Tab>
    Что блок получил — разрешённые значения переменных, ссылочные выводы и переменные окружения. Ключи API автоматически скрываются.
  </Tab>
</Tabs>

## Снимки рабочего процесса

**View Snapshot** открывает замороженную копию рабочего процесса в точности такой, какой она была во время выполнения — структура, состояния блоков, соединения — и каждый блок кликабелен для просмотра его входных и выходных данных. Это то, как вы отлаживаете запуск рабочего процесса, который вы с тех пор изменили.

<div className="flex justify-center">
  <Image
    src="/static/logs/logs-frozen-canvas.png"
    alt="Снимок рабочего процесса"
    width={600}
    height={400}
    className="my-6"
  />
</div>

<Callout type="info">
Снимки существуют для запусков после внедрения улучшенной системы журналирования. Для старых перенесённых журналов отображается «Logged State Not Found».
</Callout>

## Хранение

- **Free**: 7 дней — журналы архивируются в облачное хранилище, затем удаляются.
- **Pro / Team / Enterprise**: хранятся бессрочно.

## Далее

- [Оповещения](/logs-debugging/alerts) для уведомлений о сбоях и медленных запусках
- [Расчёт стоимости](/platform/costs) о том, сколько стоит каждый запуск
- [Внешний API](/api-reference/getting-started) для программного доступа к журналам

import { FAQ } from '@/components/ui/faq'

<FAQ items={[
  { question: "Как долго хранятся журналы запусков?", answer: "Бесплатные тарифы хранят журналы 7 дней — после этого журналы архивируются в облачное хранилище и удаляются из базы данных. Тарифы Pro, Team и Enterprise хранят журналы бессрочно без автоматической очистки." },
  { question: "Какие данные фиксируются в каждом журнале запуска?", answer: "Каждая запись журнала включает идентификатор запуска, идентификатор рабочего процесса, тип триггера, временные метки начала и окончания, общую длительность в миллисекундах, разбивку затрат (общая стоимость, количество токенов и разбивка по моделям), данные запуска с интервалами трассировки, итоговый вывод и все связанные файлы. Боковая панель деталей журнала позволяет просматривать входные и выходные данные на уровне блоков." },
  { question: "Видны ли ключи API в журналах?", answer: "Нет. Ключи API и учётные данные автоматически скрываются на вкладке ввода журнала для безопасности. Вы можете безопасно проверять входные данные блоков, не раскрывая конфиденциальные значения." },
  { question: "Что такое снимок рабочего процесса?", answer: "Замороженная копия структуры рабочего процесса (блоки, соединения и конфигурация), зафиксированная во время выполнения, чтобы вы могли видеть точное состояние, лежащее в основе конкретного запуска — полезно для отладки рабочих процессов, которые были изменены с тех пор." },
  { question: "Могу ли я получить доступ к журналам программно?", answer: "Да. Внешний API предоставляет эндпоинты для запроса журналов с фильтрацией по рабочему процессу, временному диапазону, типу триггера, длительности, стоимости и модели. Вы также можете настроить уведомления через вебхук, email или Slack для оповещений в реальном времени при завершении запусков." },
  { question: "Что делает живой режим на странице журналов?", answer: "Он обновляет страницу журналов в реальном времени, так что новые записи появляются по мере их записи — полезно при развёртываниях или при мониторинге активных рабочих процессов." },
]} />
