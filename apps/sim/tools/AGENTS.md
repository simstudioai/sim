# Tools Scope

Applies to integration tool definitions under `apps/sim/tools/**`. Author tools with the `/add-tools` skill (file layout, param visibility tiers, `transformResponse`, registration) starting from the service's API docs. Hard rules shared with blocks and triggers are in `.claude/rules/sim-integrations.md`.
