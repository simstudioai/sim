# Triggers Scope

Applies to trigger definitions under `apps/sim/triggers/**`. Author triggers with the `/add-trigger` skill (webhook model research, per-service layout, shared helpers, registration) and check them with `/validate-trigger`. Hard rules shared with tools and blocks are in `.claude/rules/sim-integrations.md`.
