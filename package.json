{
  "devDependencies": {
    "@next/env": "^15.3.2",
    "turbo": "^2.5.3"
  },
  "packageManager": "npm@11.3.0",
  "name": "simstudio",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test"
  },
  "overrides": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  }
}
